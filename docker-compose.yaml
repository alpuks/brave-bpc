services:
  db:
    image: mariadb:10.5
    container_name: db
    ports:
      - '3308:3306'
    volumes:
      - ./db:/var/lib/mysql
    environment:
      MYSQL_USER: local
      MYSQL_PASSWORD: local
      MYSQL_DATABASE: local
      MYSQL_ROOT_PASSWORD: local

volumes:
  db:

